winged:
  checking:
    streams:
      autoReportOnStartup: false

spring:
  cloud:
    stream:
      defaultBinder: test
      bindings:
        check-in:
          destination: topic-jhipster
          contentType: application/json
        check-out:
          destination: topic-jhipster
          contentType: application/json
